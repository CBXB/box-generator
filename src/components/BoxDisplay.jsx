import React, { useState } from 'react';

const BoxDisplay = (props) => {
    return(

        <div style={{width: "100px", height: "100px", backgroundColor: props.bgColor}}>

        </div>
        
    )
    
} 

export default BoxDisplay;